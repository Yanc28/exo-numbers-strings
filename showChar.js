

let text = `C'était à Mégara, faubourg de Carthage, dans les jardins d'Hamilcar.`
let auteur = `Gustave Flaubert`
let livre = `Salammbô`
console.log(`Ce texte ${text} est extrait du livre ${livre} de ${auteur}, et contient ${text.length} characters.`)



// programme that shows line by line what index is attach to each character
for (let i = 0; i < text.length; ++i) {
  console.log(`character ${text[i]} is at index: ${i}`)
}










